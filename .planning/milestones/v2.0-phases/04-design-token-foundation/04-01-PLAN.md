---
phase: 04-design-token-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main.tsx
  - src/styles/globals.css
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - DTKN-04
  - DTKN-05

must_haves:
  truths:
    - "Japanese text (CJK) in article titles renders in Noto Sans JP Variable with no FOIT flash on cold launch"
    - "Typography hierarchy uses three distinct weights: 300 (meta/light), 400 (body), 600 (title/heading)"
    - "The font-family CSS variable --font-jp is defined and applied to body element"
  artifacts:
    - path: "src/main.tsx"
      provides: "Font CSS import for @fontsource-variable/noto-sans-jp/wght.css"
    - path: "src/styles/globals.css"
      provides: "--font-jp CSS variable and body font-family declaration"
    - path: "package.json"
      provides: "@fontsource-variable/noto-sans-jp dependency"
  key_links:
    - from: "src/main.tsx"
      to: "node_modules/@fontsource-variable/noto-sans-jp/wght.css"
      via: "import statement"
      pattern: 'import "@fontsource-variable/noto-sans-jp/wght.css"'
    - from: "src/styles/globals.css"
      to: "body"
      via: "font-family: var(--font-jp)"
      pattern: "--font-jp.*Noto Sans JP Variable"
---

<objective>
Install Noto Sans JP Variable font and define the typography token hierarchy.

Purpose: Establish the CJK font foundation (DTKN-04) and 3-weight typography system (DTKN-05) so that Japanese text renders correctly across all Wings. This work is independent of the Stitch palette session and can proceed immediately.

Output: `@fontsource-variable/noto-sans-jp` installed, font imported in main.tsx, `--font-jp` CSS variable defined, body font-family set.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-design-token-foundation/04-CONTEXT.md
@.planning/phases/04-design-token-foundation/04-RESEARCH.md
@.planning/phases/04-design-token-foundation/04-UI-SPEC.md
@src/styles/globals.css
@src/main.tsx

<interfaces>
<!-- Font package details (from RESEARCH.md verified source) -->
Package: @fontsource-variable/noto-sans-jp
Version: 5.2.10
License: OFL-1.1
Import path: "@fontsource-variable/noto-sans-jp/wght.css"
Font-family name in @font-face: "Noto Sans JP Variable"
Built-in font-display: swap (no manual override needed)
Ships 124 woff2 files with unicode-range subsetting (browser loads only needed chunks)
Weight axis: 100-900 (variable)

<!-- Current globals.css structure -->
Line 1-3: @import statements (tailwindcss, animations.css, components.css)
Line 6-57: :root block with Surface, Content, Accent, Surface overlays, Shadows, Legacy aliases
Line 59-64: html/body/#root background
Line 66-87: Accessibility (.skip-link, :root --focus-ring)
Line 89-99: .sr-only
Line 101-112: @media prefers-reduced-motion
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Install Noto Sans JP Variable font and add import</name>
  <files>package.json, package-lock.json, src/main.tsx</files>
  <read_first>
    - src/main.tsx (current import structure — add font import AFTER globals.css import)
    - package.json (verify @fontsource-variable/noto-sans-jp is not already installed)
  </read_first>
  <action>
    1. Install the font package (per D-06 — Noto Sans JP only, per D-09 — @fontsource-variable):
       ```bash
       npm install @fontsource-variable/noto-sans-jp
       ```
    2. Verify the installed version is 5.2.10+ and the `wght.css` file exists:
       ```bash
       npm ls @fontsource-variable/noto-sans-jp
       ls node_modules/@fontsource-variable/noto-sans-jp/wght.css
       ```
    3. Add the font CSS import to `src/main.tsx` AFTER the existing `./styles/globals.css` import.
       The file should become:
       ```typescript
       import React from 'react';
       import ReactDOM from 'react-dom/client';
       import App from './App';
       import './styles/globals.css';
       import '@fontsource-variable/noto-sans-jp/wght.css';

       ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
         <React.StrictMode>
           <App />
         </React.StrictMode>,
       );
       ```
    CRITICAL: Import path is `@fontsource-variable/noto-sans-jp/wght.css` (NOT `index.css`, NOT `@fontsource/noto-sans-jp`). The `wght.css` entry point loads the variable weight axis (100-900) with font-display: swap already embedded. Do NOT add any manual @font-face or font-display declarations.
  </action>
  <verify>
    <automated>npm ls @fontsource-variable/noto-sans-jp && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npm ls @fontsource-variable/noto-sans-jp` shows version 5.2.x installed
    - `src/main.tsx` contains the line `import '@fontsource-variable/noto-sans-jp/wght.css';`
    - `src/main.tsx` contains the line `import './styles/globals.css';` BEFORE the font import
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Font package installed, import added to main.tsx, TypeScript compiles clean</done>
</task>

<task type="auto">
  <name>Task 2: Define --font-jp CSS variable and typography hierarchy</name>
  <files>src/styles/globals.css</files>
  <read_first>
    - src/styles/globals.css (current :root block structure, line count — currently 112 lines)
    - .planning/phases/04-design-token-foundation/04-UI-SPEC.md (Typography section — font-family declaration, 4 type sizes, 3 weights)
    - .planning/phases/04-design-token-foundation/04-RESEARCH.md (Pattern 4: Font Import, Pitfall 4: Font Name Mismatch)
  </read_first>
  <action>
    Add typography CSS variables to the existing `:root` block in `src/styles/globals.css`.

    1. Add a Typography section AFTER the Shadows section (after line 38 `--shadow-lg`) and BEFORE the Legacy aliases section:
       ```css
       /* Typography */
       --font-jp: "Noto Sans JP Variable", system-ui, sans-serif;
       --font-weight-light: 300;
       --font-weight-regular: 400;
       --font-weight-semibold: 600;
       ```

    2. Add a `body` font-family rule. Insert AFTER the existing `html, body, #root` block (after line 64):
       ```css
       body {
         font-family: var(--font-jp);
       }
       ```

    CRITICAL per D-08: Only 3 weights (300, 400, 600). The font-family name MUST be exactly `"Noto Sans JP Variable"` (with "Variable" suffix) — this matches the @font-face declarations in the wght.css package. Using `"Noto Sans JP"` (without Variable) causes a silent FOIT where the browser permanently falls back to system-ui.

    Per D-07: Do NOT add any `font-display` property — the package already includes `font-display: swap` in every @font-face block. Adding a manual override would be redundant and could conflict.

    Do NOT add font-size variables at this time — the type scale sizes (11px, 13px, 15px, 18px per UI-SPEC) remain inline in component CSS as they are today. Phase 4 establishes the font-family and weight tokens only.
  </action>
  <verify>
    <automated>npm run check && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/styles/globals.css` contains `--font-jp: "Noto Sans JP Variable", system-ui, sans-serif;`
    - `src/styles/globals.css` contains `--font-weight-light: 300;`
    - `src/styles/globals.css` contains `--font-weight-regular: 400;`
    - `src/styles/globals.css` contains `--font-weight-semibold: 600;`
    - `src/styles/globals.css` contains `body { font-family: var(--font-jp); }` (or equivalent block)
    - `src/styles/globals.css` does NOT contain `font-display` anywhere (package handles this)
    - `npm run check` exits 0 (Biome clean)
    - `npm run typecheck` exits 0 (TypeScript clean)
    - File line count stays under 280 lines
  </acceptance_criteria>
  <done>Typography CSS variables defined with correct Noto Sans JP Variable font-family name. 3-weight system (300/400/600) tokenized. body element uses --font-jp. Biome and TypeScript checks pass.</done>
</task>
</tasks>

<verification>
1. `npm run check` passes (Biome lint/format clean)
2. `npm run typecheck` passes (TypeScript clean)
3. `npm ls @fontsource-variable/noto-sans-jp` shows installed version
4. `grep -n "Noto Sans JP Variable" src/styles/globals.css` returns the --font-jp definition
5. `grep -n "fontsource-variable/noto-sans-jp/wght.css" src/main.tsx` returns the import line
6. `npx vitest run` passes (all 157 existing tests green)
</verification>

<success_criteria>
- Noto Sans JP Variable font is installed as npm dependency and imported in main.tsx
- --font-jp CSS variable correctly declares "Noto Sans JP Variable" font-family
- Three weight tokens (300, 400, 600) are defined as CSS variables
- body element uses var(--font-jp) for font-family
- Zero Biome errors, zero TypeScript errors, all existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-design-token-foundation/04-01-SUMMARY.md`
</output>
