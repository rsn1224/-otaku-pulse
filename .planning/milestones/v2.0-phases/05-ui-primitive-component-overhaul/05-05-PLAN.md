---
phase: 05-ui-primitive-component-overhaul
plan: 05
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-03", "05-04"]
files_modified: []
autonomous: false
requirements: [COMP-01, COMP-02, COMP-03, COMP-04, COMP-05, COMP-06, COMP-07, PERF-03]

must_haves:
  truths:
    - "All 7 UI primitives render with new neon/glass variants and test suite is fully green"
    - "Sidebar shows lit-up active state with neon glow and 3px left-border accent"
    - "DiscoverCard renders in poster-ratio 2:3 cover-art mode"
    - "Glassmorphism blur does not exceed blur budget (max 2 blurred layers, each under 15% viewport)"
    - "AI badge chip shows purple-to-blue gradient on AI-processed cards"
    - "Empty states show anime-culture CSS motifs"
    - "Modal focus-trap works: Tab cycles within overlay, Escape closes it"
  artifacts: []
  key_links: []
---

<objective>
Final verification checkpoint for Phase 5. Run all automated checks and request human visual verification of the rebuilt UI components, glassmorphism GPU performance, and focus-trap accessibility.

Purpose: Confirms all COMP-01 through COMP-07 and PERF-03 requirements are met before phase completion.
Output: Verified phase with human sign-off on visual quality and accessibility.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-ui-primitive-component-overhaul/05-CONTEXT.md
@.planning/phases/05-ui-primitive-component-overhaul/05-UI-SPEC.md
@.planning/phases/05-ui-primitive-component-overhaul/05-01-SUMMARY.md
@.planning/phases/05-ui-primitive-component-overhaul/05-02-SUMMARY.md
@.planning/phases/05-ui-primitive-component-overhaul/05-03-SUMMARY.md
@.planning/phases/05-ui-primitive-component-overhaul/05-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated verification suite</name>
  <files></files>
  <read_first>
    - src/components/ui/Badge.tsx
    - src/components/ui/Button.tsx
    - src/components/ui/Modal.tsx
    - src/components/layout/AppShell.tsx
    - src/components/common/Toast.tsx
  </read_first>
  <action>
Run the full automated verification suite and collect results:

1. TypeScript compilation: `npm run typecheck`
2. Biome lint/format: `npm run check`
3. All tests: `npm run test`
4. Verify no React.FC in UI primitives: `grep -r "React.FC" src/components/ui/`
5. Verify no .join(' ') in UI primitives: `grep -r "\.join(' ')" src/components/ui/`
6. Verify no inline SVG paths in AppShell: `grep "icon: 'M" src/components/layout/AppShell.tsx`
7. Verify no Tailwind default colors in Toast: `grep -E "bg-(green|red|blue)-" src/components/common/Toast.tsx`
8. Verify no hardcoded hex in modified files: `grep -rn "#[0-9a-fA-F]\{6\}" src/components/ui/ src/components/layout/AppShell.tsx src/components/common/Toast.tsx` (should only return intentional values like white/15% in bold-glass)
9. Verify CVA usage: `grep -l "import.*cva" src/components/ui/*.tsx`
10. Verify bold-glass classes exist: `grep "bold-glass" src/styles/components.css`
11. Verify retro class hooks: `grep "retro-decoration" src/components/common/EmptyState.tsx`
12. Verify focus-trap still wired in Modal: `grep "useFocusTrap" src/components/ui/Modal.tsx`

Report all results. If any check fails, fix before proceeding to checkpoint.
  </action>
  <verify>
    <automated>cd /c/dev/otaku-pulse && npm run typecheck && npm run check && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0
    - `npm run check` exits 0
    - `npm run test` exits 0 with all 76+ tests passing
    - `grep -r "React.FC" src/components/ui/` returns zero matches
    - `grep -r "\.join(' ')" src/components/ui/` returns zero matches
    - `grep "icon: 'M" src/components/layout/AppShell.tsx` returns zero matches
    - `grep -l "import.*cva" src/components/ui/*.tsx` returns at least Badge.tsx, Button.tsx, Card.tsx
    - `grep "useFocusTrap" src/components/ui/Modal.tsx` returns a match
  </acceptance_criteria>
  <done>All automated checks pass with zero errors across TypeScript, Biome, and test suite.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification of Phase 5 rebuild</name>
  <files></files>
  <action>
Present the following verification checklist to the user. The app must be running via `npm run tauri dev`.

Verify these 10 areas covering all 8 requirements (COMP-01 through COMP-07, PERF-03):

1. **Sidebar (COMP-04):** Click through all 5 Wings. Active Wing shows neon purple glow + 3px left border + primary-soft background. Inactive Wings show muted icon color. Hover on inactive shows subtle surface-hover background. Icons are clean lucide-react vectors.
2. **DiscoverCard (COMP-02):** Cards show 2:3 poster thumbnail on the left in collapsed state. Expand a card — thumbnail moves to full-width at top. Cards without cover art show colored gradient + category icon fallback. Cards have content-type colored left border.
3. **AI Badge (COMP-05):** Expand a card with AI summary. Purple-to-blue gradient "AI" badge chip appears.
4. **Glassmorphism Modal (COMP-03):** Open a modal. Dialog panel has frosted glass effect. Backdrop is near-opaque dark (no blur on backdrop itself).
5. **Glassmorphism Toast (COMP-03):** Trigger a notification. Toast has frosted glass background with left-border semantic color.
6. **Focus trap (COMP-01):** With modal open, Tab cycles focus within the modal. Escape closes it.
7. **GPU blur performance (PERF-03):** With DeepDive expanded, scrolling remains smooth. No long frames > 50ms in DevTools Performance tab.
8. **Empty States (COMP-07):** Navigate to Saved Wing (or any empty wing). CSS art motif and lucide icon visible.
9. **Section Headers (COMP-06):** Left-border accent visible with glow effect.
10. **UI Primitives (COMP-01):** Buttons, badges, cards, inputs, toggles all render with updated design language.
  </action>
  <verify>User confirms "approved" or reports issues</verify>
  <done>Human verified all visual, accessibility, and performance requirements for Phase 5.</done>
</task>

</tasks>

<verification>
All automated checks pass. Human verified visual quality, glassmorphism performance, and accessibility.
</verification>

<success_criteria>
- All automated checks pass (typecheck, lint, tests)
- Human verified all 8 requirement areas (COMP-01 through COMP-07, PERF-03)
- No visual regressions, GPU performance acceptable, focus-trap functional
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-primitive-component-overhaul/05-05-SUMMARY.md`
</output>
