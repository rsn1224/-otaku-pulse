---
phase: 01-foundation-correctness
plan: 03
type: execute
wave: 2
depends_on:
  - 01-02
files_modified:
  - src-tauri/Cargo.toml
  - src-tauri/src/services/discover_queries.rs
  - src/lib/articleFilter.ts
  - src/stores/useDiscoverStore.ts
autonomous: true
requirements:
  - DEP-01
  - DEP-02
  - DEP-03
  - FRNT-01

must_haves:
  truths:
    - "feed-rs, reqwest, and sqlx versions are pinned with ~ prefix in Cargo.toml"
    - "sqlx offline metadata exists in .sqlx/ directory and is committed to Git"
    - "applyMuteFilters logic executes in Rust SQL query, not in TypeScript"
    - "getHighlightKeywords remains in TypeScript (display-only logic per D-09)"
    - "CI can build with SQLX_OFFLINE=true without a live database"
  artifacts:
    - path: "src-tauri/Cargo.toml"
      provides: "Pinned versions: ~2.1 (feed-rs), ~0.12 (reqwest), ~0.8 (sqlx)"
    - path: "src-tauri/.sqlx/"
      provides: "Offline query metadata for CI builds"
    - path: "src-tauri/src/services/discover_queries.rs"
      provides: "SQL WHERE clause excluding muted keywords at DB level"
    - path: "src/lib/articleFilter.ts"
      provides: "applyMuteFilters removed, getHighlightKeywords retained"
  key_links:
    - from: "src/stores/useDiscoverStore.ts"
      to: "src/lib/articleFilter.ts"
      via: "Store no longer calls applyMuteFilters (call sites removed)"
      pattern: "applyMuteFilters"
    - from: "src-tauri/src/services/discover_queries.rs"
      to: "src-tauri/migrations/008_phase1_foundation.sql"
      via: "Query reads keyword_filters table which existed before 008 but 008 must be applied for content_hash changes"
      pattern: "keyword_filters.*mute"
---

<objective>
Pin external I/O dependency versions, generate sqlx offline metadata, and move mute filtering from TypeScript to Rust backend SQL.

Purpose: Dependency pinning prevents silent breakage from patch updates to feed-rs/reqwest/sqlx. sqlx offline mode enables CI builds without a live DB. Moving mute filtering to SQL eliminates client/server filter logic duplication.

Output: Cargo.toml updated, .sqlx/ directory created, mute filter moved to Rust query, articleFilter.ts simplified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-correctness/01-CONTEXT.md
@.planning/phases/01-foundation-correctness/01-RESEARCH.md
@.claude/rules/typescript.md

<interfaces>
From src-tauri/Cargo.toml:
  feed-rs = "2.1"
  reqwest = { version = "0.12", features = ["rustls-tls", "json"] }
  sqlx = { version = "0.8", features = ["sqlite", "runtime-tokio", "macros", "migrate"] }

From src/lib/articleFilter.ts:
  export function applyMuteFilters(articles: Article[], muteKeywords: string[]): Article[]
  export function getHighlightKeywords(title: string, keywords: string[]): string[]

From src-tauri/src/services/discover_queries.rs:
  pub async fn get_discover_feed(db: &SqlitePool, tab: &str, offset: i64, limit: i64) -> Result<DiscoverFeedResult, AppError>
  -- The SQL query inside selects from articles WHERE is_duplicate = 0 AND is_read = 0

From src-tauri/migrations/002_keyword_filters.sql:
  CREATE TABLE keyword_filters (id INTEGER PRIMARY KEY, keyword TEXT, filter_type TEXT, ...)
  -- filter_type = "mute" for muted keywords
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Pin dependency versions + mute filter migration to Rust</name>
  <files>src-tauri/Cargo.toml, src-tauri/src/services/discover_queries.rs, src/lib/articleFilter.ts, src/stores/useDiscoverStore.ts</files>
  <read_first>
    - src-tauri/Cargo.toml (full file -- need to see all dependency version specs)
    - src-tauri/src/services/discover_queries.rs (full file -- need to see get_discover_feed SQL query)
    - src/lib/articleFilter.ts (full file -- need to see applyMuteFilters and getHighlightKeywords)
    - src/stores/useDiscoverStore.ts (full file -- need to find applyMuteFilters call sites)
    - src-tauri/migrations/002_keyword_filters.sql (see keyword_filters table schema)
  </read_first>
  <action>
**DEP-01, DEP-02 -- Dependency pinning (per D-14):**

1. In `src-tauri/Cargo.toml`, change these three dependency versions:
   - `feed-rs = "2.1"` to `feed-rs = "~2.1"`
   - `reqwest = { version = "0.12", ... }` to `reqwest = { version = "~0.12", ... }`
   - `sqlx = { version = "0.8", ... }` to `sqlx = { version = "~0.8", ... }`

   Do NOT change any other dependencies. tokio, serde, chrono, etc. remain as-is per D-14.

**FRNT-01 -- Move applyMuteFilters to Rust SQL (per D-08, D-09):**

2. In `discover_queries.rs`, find the `get_discover_feed` function's SQL query. Add a `NOT EXISTS` subquery to exclude articles matching muted keywords:
   ```sql
   AND NOT EXISTS (
       SELECT 1 FROM keyword_filters kf
       WHERE kf.filter_type = 'mute'
         AND (LOWER(a.title) LIKE '%' || LOWER(kf.keyword) || '%'
              OR LOWER(COALESCE(a.summary, '')) LIKE '%' || LOWER(kf.keyword) || '%')
   )
   ```
   Place this clause after the existing `WHERE is_duplicate = 0 AND is_read = 0` conditions and before the `ORDER BY` clause. The table alias `a` should match whatever alias the existing query uses for the articles table.

   If the current query also filters on `ai_summary` (matching the TS `applyMuteFilters` which checks `aiSummary`), also add:
   ```sql
   OR LOWER(COALESCE(a.ai_summary, '')) LIKE '%' || LOWER(kf.keyword) || '%'
   ```
   Check the actual column name by reading the articles table schema.

3. In `src/lib/articleFilter.ts`:
   - Delete the `applyMuteFilters` function entirely
   - Keep the `getHighlightKeywords` function (per D-09, it stays in frontend)
   - Update the file's exports accordingly

4. In `src/stores/useDiscoverStore.ts` (and any other files that import `applyMuteFilters`):
   - Remove the import of `applyMuteFilters` from `articleFilter`
   - Remove any call to `applyMuteFilters(...)` -- the backend query now handles this
   - Search the entire `src/` directory for other imports of `applyMuteFilters` and remove them

5. Run `npx biome check --apply src/lib/articleFilter.ts src/stores/useDiscoverStore.ts` to fix formatting.
  </action>
  <verify>
    <automated>cd src-tauri && cargo check 2>&1 | head -20 && cd .. && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Cargo.toml contains `feed-rs = "~2.1"` (grep confirms)
    - Cargo.toml contains `version = "~0.12"` in the reqwest line
    - Cargo.toml contains `version = "~0.8"` in the sqlx line
    - discover_queries.rs contains `keyword_filters` in the SQL query
    - discover_queries.rs contains `filter_type = 'mute'` in the SQL
    - articleFilter.ts does NOT contain `applyMuteFilters` function (grep returns empty)
    - articleFilter.ts DOES contain `getHighlightKeywords` function
    - No TypeScript file imports `applyMuteFilters` (grep across src/ returns empty)
    - `cargo check` succeeds
    - `npx tsc --noEmit` succeeds (no type errors from removed function)
  </acceptance_criteria>
  <done>Dependencies pinned, mute filtering moved to SQL, TypeScript simplified</done>
</task>

<task type="auto">
  <name>Task 2: Generate sqlx offline metadata</name>
  <files>src-tauri/.sqlx/</files>
  <read_first>
    - src-tauri/Cargo.toml (confirm sqlx features include "offline" or that offline mode works with existing features)
    - .gitignore (check if .sqlx/ is gitignored -- it should NOT be)
  </read_first>
  <action>
**IMPORTANT: Task 2 must execute AFTER Task 1 is complete and verified. Task 1 adds the mute filter SQL to discover_queries.rs, and `cargo sqlx prepare` must capture metadata for that new query. Running prepare before Task 1 would produce incomplete .sqlx/ metadata.**

**DEP-03 -- sqlx offline mode (per D-15):**

1. Install sqlx-cli if not present:
   ```bash
   cargo install sqlx-cli --no-default-features --features sqlite
   ```

2. Create a temporary dev database with all migrations applied:
   ```bash
   cd src-tauri
   export DATABASE_URL="sqlite:./dev_temp.db?mode=rwc"
   cargo sqlx database create
   cargo sqlx migrate run --source ./migrations
   ```

3. Generate offline query metadata:
   ```bash
   cd src-tauri
   export DATABASE_URL="sqlite:./dev_temp.db"
   cargo sqlx prepare
   ```
   This creates the `.sqlx/` directory with JSON files for each compile-time checked query.

4. Verify the build works in offline mode:
   ```bash
   cd src-tauri
   SQLX_OFFLINE=true cargo check
   ```

5. Clean up the temporary database:
   ```bash
   rm -f src-tauri/dev_temp.db src-tauri/dev_temp.db-wal src-tauri/dev_temp.db-shm
   ```

6. Ensure `.sqlx/` is NOT in `.gitignore`. If it is listed there, remove the entry.

7. Stage `.sqlx/` for git commit.

**NOTE:** This task depends on Plan 02 being complete because migration 008 must exist before `cargo sqlx migrate run` and `cargo sqlx prepare` are executed. The prepare step captures metadata for ALL queries including those that reference columns added in 008. It also depends on Task 1 of this plan being complete so the new mute filter SQL in discover_queries.rs is captured.
  </action>
  <verify>
    <automated>cd src-tauri && test -d .sqlx && ls .sqlx/*.json 2>/dev/null | head -5 && echo "sqlx offline metadata exists" || echo "MISSING .sqlx/ directory"</automated>
  </verify>
  <acceptance_criteria>
    - Directory `src-tauri/.sqlx/` exists
    - `.sqlx/` contains at least one `.json` file (query metadata)
    - `SQLX_OFFLINE=true cargo check` succeeds in src-tauri directory
    - `dev_temp.db` does NOT exist after cleanup
    - `.gitignore` does NOT contain `.sqlx`
  </acceptance_criteria>
  <done>sqlx offline metadata generated, CI can build without live database</done>
</task>
</tasks>

<verification>
1. `cd src-tauri && cargo check` -- no compile errors
2. `cd src-tauri && cargo clippy -- -D warnings` -- no warnings
3. `cd src-tauri && SQLX_OFFLINE=true cargo check` -- offline build succeeds
4. `npx tsc --noEmit` -- no TypeScript type errors
5. `npx biome check src/` -- no lint errors
6. `grep -rn "applyMuteFilters" src/` -- returns empty (fully removed)
7. `grep -n "getHighlightKeywords" src/lib/articleFilter.ts` -- returns match (retained)
8. `grep "~2.1" src-tauri/Cargo.toml` -- feed-rs pinned
9. `grep "~0.12" src-tauri/Cargo.toml` -- reqwest pinned
10. `grep "~0.8" src-tauri/Cargo.toml` -- sqlx pinned
</verification>

<success_criteria>
- feed-rs, reqwest, sqlx pinned with ~ prefix
- .sqlx/ directory exists with query metadata
- SQLX_OFFLINE=true cargo check passes
- applyMuteFilters removed from TypeScript, mute filtering in SQL
- getHighlightKeywords retained in TypeScript
- No compile errors in Rust or TypeScript
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-correctness/01-03-SUMMARY.md`
</output>
