---
phase: 04-design-token-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/globals.css
  - src/styles/components.css
  - src/components/common/ArticleReader.tsx
  - src/components/common/KeyboardHelpModal.tsx
  - src/components/common/RelatedArticles.tsx
  - src/components/common/Toast.tsx
  - src/components/discover/CardActions.tsx
  - src/components/discover/CardHeader.tsx
  - src/components/discover/CardSkeleton.tsx
  - src/components/discover/CardSummary.tsx
  - src/components/discover/DeepDivePanel.tsx
  - src/components/discover/DiscoverCard.tsx
  - src/components/discover/HighlightsSection.tsx
  - src/components/discover/UniversalTabs.tsx
  - src/components/layout/AppShell.tsx
  - src/components/layout/TopBarSearch.tsx
  - src/components/onboarding/OnboardingWizard.tsx
  - src/components/onboarding/PreferenceSuggestion.tsx
  - src/components/onboarding/TagInputStep.tsx
  - src/components/profile/AdvancedSection.tsx
  - src/components/profile/FeedsSection.tsx
  - src/components/profile/ProfileSection.tsx
  - src/components/reader/ArticleBody.tsx
  - src/components/reader/ReaderHeader.tsx
  - src/components/schedule/AiringCard.tsx
  - src/components/schedule/GameReleaseCard.tsx
  - src/components/schedule/GameViews.tsx
  - src/components/schedule/ScheduleGridView.tsx
  - src/components/schedule/ScheduleToggleGroup.tsx
  - src/components/ui/Badge.tsx
  - src/components/ui/Button.tsx
  - src/components/ui/Card.tsx
  - src/components/ui/Input.tsx
  - src/components/ui/Modal.tsx
  - src/components/ui/Spinner.tsx
  - src/components/ui/ToggleGroup.tsx
  - src/components/wings/ArticleList.tsx
  - src/components/wings/DiscoverWing.tsx
  - src/components/wings/LibraryWing.tsx
  - src/components/wings/ProfileWing.tsx
  - src/components/wings/SavedWing.tsx
  - src/components/wings/ScheduleWing.tsx
autonomous: true
requirements:
  - DTKN-06

must_haves:
  truths:
    - "Running grep for all 16 legacy aliases returns zero matches in src/ source files"
    - "The Legacy aliases section in globals.css is completely removed"
    - "The app compiles with zero TypeScript and Biome errors after alias removal"
    - "All 157 existing tests pass after alias removal"
  artifacts:
    - path: "src/styles/globals.css"
      provides: "Clean :root without Legacy aliases section"
    - path: "src/styles/components.css"
      provides: "All var(--legacy) replaced with var(--recommended-token)"
  key_links:
    - from: "src/styles/components.css"
      to: "src/styles/globals.css :root"
      via: "var(--surface-container) etc."
      pattern: "var\\(--surface"
    - from: "src/components/**/*.tsx"
      to: "src/styles/globals.css :root"
      via: "Tailwind bg-(--token) / text-(--token) syntax"
      pattern: "(bg|text|border)-\\(--"
---

<objective>
Replace all 212 legacy CSS alias usages across 28+ source files with their recommended design tokens, then delete the Legacy aliases section from globals.css.

Purpose: Complete the legacy alias migration (DTKN-06, per D-10 one-shot batch replacement, per D-11 grep-then-replace workflow). This eliminates the indirection layer and ensures all CSS references point directly to the canonical token names before Phase 5 component work begins.

Output: Zero legacy alias references in src/. Legacy aliases block removed from globals.css. All lint/type/test gates green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-design-token-foundation/04-CONTEXT.md
@.planning/phases/04-design-token-foundation/04-RESEARCH.md
@.planning/phases/04-design-token-foundation/04-UI-SPEC.md
@src/styles/globals.css
@src/styles/components.css

<interfaces>
<!-- Legacy Alias Replacement Mapping (from RESEARCH.md + UI-SPEC.md) -->
<!-- CSS var() replacements (components.css and any .css file): -->
var(--bg-primary)       -> var(--surface)
var(--bg-card)          -> var(--surface-container)
var(--bg-card-hover)    -> var(--surface-container-high)
var(--bg-secondary)     -> var(--surface-elevated)      [D-12: absorbed]
var(--bg-deepdive)      -> var(--surface-elevated)      [D-12: absorbed]
var(--border)           -> var(--surface-container-highest)
var(--border-hover)     -> var(--outline-variant)
var(--accent)           -> var(--primary)
var(--accent-soft)      -> var(--primary-soft)
var(--accent-glow)      -> var(--primary-glow)
var(--text-primary)     -> var(--on-surface)
var(--text-secondary)   -> var(--on-surface-variant)
var(--text-tertiary)    -> var(--outline)
var(--text-source)      -> var(--on-surface-variant)
var(--badge-hot)        -> var(--error)
var(--badge-new)        -> var(--secondary)

<!-- Tailwind arbitrary syntax replacements (TSX files): -->
bg-(--bg-primary)       -> bg-(--surface)
bg-(--bg-card)          -> bg-(--surface-container)
bg-(--bg-card-hover)    -> bg-(--surface-container-high)
bg-(--bg-secondary)     -> bg-(--surface-elevated)
bg-(--bg-deepdive)      -> bg-(--surface-elevated)
border-(--border)       -> border-(--surface-container-highest)
border-(--border-hover) -> border-(--outline-variant)
text-(--text-primary)   -> text-(--on-surface)
text-(--text-secondary) -> text-(--on-surface-variant)
text-(--text-tertiary)  -> text-(--outline)
text-(--text-source)    -> text-(--on-surface-variant)
bg-(--accent)           -> bg-(--primary)
text-(--accent)         -> text-(--primary)
border-(--accent)       -> border-(--primary)
bg-(--accent-soft)      -> bg-(--primary-soft)
bg-(--accent-glow)      -> bg-(--primary-glow)
text-(--badge-hot)      -> text-(--error)
border-(--badge-hot)    -> border-(--error)
text-(--badge-new)      -> text-(--secondary)

NOTE on --surface-elevated: This token does NOT exist in globals.css yet. It will be added in Plan 03
when the Stitch palette is committed. For this plan, use `--surface-container` as a temporary target
for --bg-secondary and --bg-deepdive references (the existing value #131319 is closest to --surface-container #19191f).
Plan 03 Task 1 will define --surface-elevated with the Stitch-confirmed value, and these references
will already be pointing at a valid token.

CORRECTION: Actually, to keep things clean and avoid a temporary mismatch, add a single
`--surface-elevated: #131319;` line in the :root block as part of this plan's cleanup. This preserves
the exact current visual appearance (D-12 says absorb into nearest layer, but we keep the value
until Stitch confirms). Plan 03 will update the HEX value.
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Batch replace all legacy alias references across src/</name>
  <files>src/styles/components.css, src/components/**/*.tsx</files>
  <read_first>
    - src/styles/components.css (all legacy alias references in CSS — this file has the most hits)
    - src/styles/globals.css (the Legacy aliases section at lines 40-56 — understand current mapping)
    - .planning/phases/04-design-token-foundation/04-UI-SPEC.md (Legacy Alias Removal Contract section — full replacement mapping)
  </read_first>
  <action>
    Per D-10 (one-shot batch) and D-11 (grep-then-replace workflow):

    **Step 0: Audit** — Run grep to capture the full hit list before any changes:
    ```bash
    grep -rn "--bg-primary\|--bg-card\|--bg-secondary\|--bg-deepdive\|--bg-card-hover\|--border-hover\|--accent-soft\|--accent-glow\|--text-primary\|--text-secondary\|--text-tertiary\|--text-source\|--badge-hot\|--badge-new" src/ --include="*.tsx" --include="*.css" | grep -v "node_modules"
    ```
    Also separately grep for `--accent` and `--border` (bare, without suffix) to catch those:
    ```bash
    grep -rn "\-\-accent[^-]" src/ --include="*.tsx" --include="*.css"
    grep -rn "\-\-border[^-]" src/ --include="*.tsx" --include="*.css"
    ```

    **Step 1: CSS file replacements (components.css)** — Apply these exact substitutions in `src/styles/components.css`:
    - `var(--bg-primary)` -> `var(--surface)`
    - `var(--bg-card)` -> `var(--surface-container)`
    - `var(--bg-card-hover)` -> `var(--surface-container-high)`
    - `var(--bg-deepdive)` -> `var(--surface-elevated)`
    - `var(--bg-secondary)` -> `var(--surface-elevated)`
    - `var(--border)` -> `var(--surface-container-highest)` (ONLY exact `var(--border)`, not `var(--border-hover)`)
    - `var(--border-hover)` -> `var(--outline-variant)`
    - `var(--accent-soft)` -> `var(--primary-soft)` (replace BEFORE bare --accent to avoid double-replace)
    - `var(--accent-glow)` -> `var(--primary-glow)` (replace BEFORE bare --accent)
    - `var(--accent)` -> `var(--primary)` (bare --accent only, after -soft and -glow are done)
    - `var(--text-primary)` -> `var(--on-surface)`
    - `var(--text-secondary)` -> `var(--on-surface-variant)`
    - `var(--text-tertiary)` -> `var(--outline)`
    - `var(--text-source)` -> `var(--on-surface-variant)`
    - `var(--badge-hot)` -> `var(--error)`
    - `var(--badge-new)` -> `var(--secondary)`

    **Step 2: TSX file replacements** — Apply Tailwind arbitrary syntax replacements across ALL .tsx files in src/components/:
    Replace order matters — always replace longer aliases first to avoid partial matches:
    - `--bg-card-hover` -> `--surface-container-high` (longest --bg-card* variant first)
    - `--bg-card` -> `--surface-container`
    - `--bg-primary` -> `--surface`
    - `--bg-secondary` -> `--surface-elevated`
    - `--bg-deepdive` -> `--surface-elevated`
    - `--border-hover` -> `--outline-variant` (before bare --border)
    - `--border` -> `--surface-container-highest` (bare only — be careful not to replace inside `--border-hover` which is already done)
    - `--accent-soft` -> `--primary-soft` (before bare --accent)
    - `--accent-glow` -> `--primary-glow` (before bare --accent)
    - `--accent` -> `--primary` (bare only)
    - `--text-primary` -> `--on-surface`
    - `--text-secondary` -> `--on-surface-variant`
    - `--text-tertiary` -> `--outline`
    - `--text-source` -> `--on-surface-variant`
    - `--badge-hot` -> `--error`
    - `--badge-new` -> `--secondary`

    **Step 3: Add --surface-elevated token** — In `src/styles/globals.css`, add `--surface-elevated: #131319;` in the `:root` Surface section (after `--surface-container-highest`), to ensure the replacement target exists. This preserves the exact current visual for --bg-secondary and --bg-deepdive (per D-12). Plan 03 will update this HEX value after Stitch session.

    **Step 4: Verify zero legacy references remain** — Run the full grep gate:
    ```bash
    grep -rn "--bg-primary\|--bg-card\|--bg-secondary\|--bg-deepdive\|--bg-card-hover\|--border-hover\|--accent-soft\|--accent-glow\|--text-primary\|--text-secondary\|--text-tertiary\|--text-source\|--badge-hot\|--badge-new" src/ --include="*.tsx" --include="*.css"
    ```
    And for bare `--accent` and `--border`:
    ```bash
    grep -rn "(--accent[^-a-z]" src/ --include="*.tsx" --include="*.css"
    grep -rn "(--border[^-a-z]" src/ --include="*.tsx" --include="*.css"
    ```
    All grep commands must return zero matches (excluding globals.css Legacy section which is deleted in Task 2).

    IMPORTANT: Do NOT touch `globals.css` Legacy aliases section yet — that is Task 2. Only add the `--surface-elevated` line in the Surface section.
  </action>
  <verify>
    <automated>npm run check && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "--bg-primary" src/ --include="*.tsx" --include="*.css"` returns zero matches (excluding globals.css lines 40-56)
    - `grep -rn "--bg-card" src/ --include="*.tsx" --include="*.css"` returns zero matches (excluding globals.css lines 40-56)
    - `grep -rn "--text-primary" src/ --include="*.tsx" --include="*.css"` returns zero matches (excluding globals.css lines 40-56)
    - `grep -rn "--text-secondary" src/ --include="*.tsx" --include="*.css"` returns zero matches (excluding globals.css lines 40-56)
    - `src/styles/globals.css` :root contains `--surface-elevated: #131319;`
    - `src/styles/components.css` contains `var(--surface-container)` where it previously had `var(--bg-card)`
    - `src/styles/components.css` contains `var(--on-surface-variant)` where it previously had `var(--text-secondary)`
    - `npm run check` exits 0
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>All 212 legacy alias references replaced with recommended tokens. --surface-elevated added to :root. Biome and TypeScript checks pass.</done>
</task>

<task type="auto">
  <name>Task 2: Delete Legacy aliases section from globals.css and verify zero residual</name>
  <files>src/styles/globals.css</files>
  <read_first>
    - src/styles/globals.css (verify the Legacy aliases section is the ONLY remaining reference to legacy names — should be lines 40-56 approximately, starting with the comment `/* === Legacy aliases`)
  </read_first>
  <action>
    1. Delete the entire Legacy aliases block from `src/styles/globals.css`. This is the section starting with the comment `/* === Legacy aliases (remove in Phase 3) === */` and containing all 16 alias definitions (`--bg-primary` through `--badge-new`). Remove both the comment line and all alias variable definitions.

    2. Run the FINAL verification grep — this time with NO exclusions:
       ```bash
       grep -rn "--bg-primary\|--bg-card\|--bg-secondary\|--bg-deepdive\|--bg-card-hover\|--border-hover\|--accent-soft\|--accent-glow\|--text-primary\|--text-secondary\|--text-tertiary\|--text-source\|--badge-hot\|--badge-new" src/
       ```
       This MUST return zero matches. If any match is found, go back and replace it before proceeding.

    3. Also verify bare aliases are gone:
       ```bash
       grep -rn "(--accent[^-a-z]" src/ --include="*.tsx" --include="*.css"
       grep -rn "(--border[^-a-z]" src/ --include="*.tsx" --include="*.css"
       ```

    4. Run all quality gates:
       ```bash
       npm run check && npm run typecheck && npx vitest run
       ```

    The app should render identically because the legacy aliases were just indirections to the same tokens that now replace them directly. The only visual change: `--bg-secondary` and `--bg-deepdive` (both `#131319`) now point through `--surface-elevated: #131319` instead of being defined inline — same value, different name.
  </action>
  <verify>
    <automated>npm run check && npm run typecheck && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `src/styles/globals.css` does NOT contain the string `Legacy aliases`
    - `src/styles/globals.css` does NOT contain `--bg-primary:`
    - `src/styles/globals.css` does NOT contain `--bg-card:`
    - `src/styles/globals.css` does NOT contain `--text-primary:`
    - `src/styles/globals.css` does NOT contain `--accent:` (bare, without -soft or -glow suffix)
    - `grep -rn "--bg-primary\|--bg-card\|--text-primary\|--text-secondary\|--badge-hot" src/` returns zero matches
    - `npm run check` exits 0
    - `npm run typecheck` exits 0
    - `npx vitest run` exits 0 (all 157 tests green)
  </acceptance_criteria>
  <done>Legacy aliases section fully deleted. Zero legacy alias references remain anywhere in src/. All quality gates pass. CSS-only change causes no visual regression — same tokens, direct references.</done>
</task>
</tasks>

<verification>
1. `grep -rn "--bg-primary\|--bg-card\|--bg-secondary\|--bg-deepdive\|--bg-card-hover\|--border-hover\|--accent-soft\|--accent-glow\|--text-primary\|--text-secondary\|--text-tertiary\|--text-source\|--badge-hot\|--badge-new" src/` returns zero matches
2. `npm run check` passes (Biome clean)
3. `npm run typecheck` passes (TypeScript clean)
4. `npx vitest run` passes (all 157 existing tests green)
5. `src/styles/globals.css` no longer contains a Legacy aliases section
6. `src/styles/globals.css` :root contains `--surface-elevated: #131319;`
</verification>

<success_criteria>
- All 16 legacy CSS aliases completely removed from codebase
- 212 usages across 28+ files replaced with canonical MD3 token names
- --surface-elevated provisionally defined at #131319 (D-12 absorption target)
- globals.css Legacy aliases section deleted
- Zero Biome errors, zero TypeScript errors, all 157 existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-design-token-foundation/04-02-SUMMARY.md`
</output>
