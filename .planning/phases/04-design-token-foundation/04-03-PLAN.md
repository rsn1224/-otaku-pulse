---
phase: 04-design-token-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - src/styles/globals.css
  - design.md
autonomous: false
requirements:
  - DTKN-01
  - DTKN-02
  - DTKN-03
  - DTKN-07

must_haves:
  truths:
    - "The app renders with void-black (#0a0a0f) base surface and five visually distinct surface layers"
    - "Neon glow variables (--glow-primary, --glow-secondary, --glow-subtle) are defined in :root"
    - "Content-type accent colors (anime=purple, manga=pink, game=cyan, news=amber) are defined as CSS variables"
    - "All 4 accent colors pass WCAG AA 4.5:1 contrast against --surface-container"
    - "design.md is fully rewritten to document the new token system"
  artifacts:
    - path: "src/styles/globals.css"
      provides: "5-layer surface hierarchy, glow system, content-type accent variables, --surface-base token"
    - path: "design.md"
      provides: "Complete design system documentation with new token tables, Stitch Token Mapping"
  key_links:
    - from: "src/styles/globals.css :root --surface-base"
      to: "html, body, #root background"
      via: "background-color uses --surface (Layer 1) which sits above --surface-base (Layer 0)"
      pattern: "--surface-base.*#0a0a0f"
    - from: "src/styles/globals.css :root --surface-elevated"
      to: "src/components/**/*.tsx referencing --surface-elevated"
      via: "Plan 02 replaced --bg-secondary/--bg-deepdive with --surface-elevated; this plan updates the HEX value from #131319 placeholder"
      pattern: "--surface-elevated"
    - from: "design.md"
      to: "src/styles/globals.css :root"
      via: "design.md documents all token values; must match globals.css exactly"
      pattern: "--surface-base.*#0a0a0f"
---

<objective>
Define the complete color token system: 5-layer surface hierarchy, neon glow variables, and content-type accent colors. Then rewrite design.md to document the entire new design system.

Purpose: This plan delivers DTKN-01 (void-black surface layers), DTKN-02 (glow system), DTKN-03 (content-type accents), and DTKN-07 (design.md rewrite). It requires the Stitch mockup session to confirm HEX values before any palette tokens are committed (per D-01, D-02, D-03, D-04). The glow and accent token CSS variables defined here will be consumed by Phase 5 components.

Output: globals.css with complete new palette. design.md fully rewritten. All WCAG AA contrast verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-design-token-foundation/04-CONTEXT.md
@.planning/phases/04-design-token-foundation/04-RESEARCH.md
@.planning/phases/04-design-token-foundation/04-UI-SPEC.md
@.planning/phases/04-design-token-foundation/04-01-SUMMARY.md
@.planning/phases/04-design-token-foundation/04-02-SUMMARY.md
@src/styles/globals.css
@design.md

<interfaces>
<!-- Surface Layer Structure (from UI-SPEC.md) -->
Layer 0: --surface-base    = #0a0a0f  (void black, LOCKED by D-01)
Layer 1: --surface          = [STITCH-VALUE]  (page background)
Layer 2: --surface-container = [STITCH-VALUE]  (card/panel)
Layer 3: --surface-container-high = [STITCH-VALUE]  (hovered card)
Layer 4: --surface-container-highest = [STITCH-VALUE]  (border/divider)
Layer 5: --surface-elevated = [STITCH-VALUE]  (floating, deepdive -- replaces #131319 from Plan 02)

<!-- Glow System (from UI-SPEC.md) -->
--glow-primary:   rgba([R],[G],[B], 0.12)  (60% dominant)
--glow-secondary: rgba([R],[G],[B], 0.08)  (30% supporting)
--glow-subtle:    rgba([R],[G],[B], 0.04)  (10% ambient)

<!-- Content-Type Accents (from D-04) -->
--accent-anime:  [purple HEX from Stitch]
--accent-manga:  [pink HEX from Stitch]
--accent-game:   [cyan HEX from Stitch]
--accent-news:   [amber HEX from Stitch]

<!-- WCAG AA requirement -->
All 4 accent colors must achieve >= 4.5:1 contrast ratio against --surface-container.
Cyan and amber are highest-risk hues at this contrast threshold.
</interfaces>
</context>

<tasks>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Stitch mockup session -- confirm palette HEX values</name>
  <files>src/styles/globals.css</files>
  <action>
    BLOCKING CHECKPOINT: The Stitch mockup session must be completed by the user before Tasks 2 and 3 can proceed.

    Plans 01 and 02 have been completed: Noto Sans JP Variable font is installed, typography tokens defined, and all 212 legacy alias references replaced with canonical token names. The codebase is ready to receive the new palette values.

    The user must run a Stitch session to generate and confirm the final HEX values for:
    1. 5-layer surface hierarchy (Layers 1-5; Layer 0 is locked at #0a0a0f per D-01)
    2. Neon glow RGB channels (derived from --primary per D-03)
    3. Content-type accent colors: anime=purple, manga=pink, game=cyan, news=amber (per D-04)
    4. Any --primary color update if the Stitch session suggests one

    Steps for the user:
    1. Run a Stitch session with keywords: "anime/otaku culture, cyberpunk, void black, neon glow"
    2. Generate a 5-layer surface palette starting from #0a0a0f (void black base)
    3. Generate neon glow color scheme following 60-30-10 rule (purple primary recommended)
    4. Generate 4 content-type accent colors: anime=purple, manga=pink, game=cyan, news=amber
    5. Export to Figma for fine-tuning if needed
    6. Verify each accent color passes WCAG AA 4.5:1 against the Layer 2 surface value using https://webaim.org/resources/contrastchecker/
    7. Report ALL confirmed HEX values in this format:
       ```
       approved
       --surface-base: #0a0a0f
       --surface: #XXXXXX
       --surface-container: #XXXXXX
       --surface-container-high: #XXXXXX
       --surface-container-highest: #XXXXXX
       --surface-elevated: #XXXXXX
       --primary: #XXXXXX (or "keep #bd9dff")
       --glow-rgb: R, G, B (RGB channels for glow rgba values)
       --accent-anime: #XXXXXX
       --accent-manga: #XXXXXX
       --accent-game: #XXXXXX
       --accent-news: #XXXXXX
       ```
  </action>
  <verify>
    <automated>echo "Checkpoint: user provides Stitch-confirmed HEX values"</automated>
  </verify>
  <done>User has confirmed all palette HEX values from Stitch session. All accent colors verified at WCAG AA 4.5:1. Values ready for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Write palette tokens to globals.css -- surface layers, glow system, content-type accents</name>
  <files>src/styles/globals.css</files>
  <read_first>
    - src/styles/globals.css (current state after Plans 01+02: contains --surface-elevated: #131319 from Plan 02, typography tokens from Plan 01, no Legacy aliases section)
    - .planning/phases/04-design-token-foundation/04-UI-SPEC.md (Color section -- surface hierarchy, glow system, content-type accents)
    - .planning/phases/04-design-token-foundation/04-RESEARCH.md (Pattern 1-3, Pitfall 5 line-count overflow)
  </read_first>
  <action>
    Using the HEX values confirmed in Task 1's checkpoint, update `src/styles/globals.css` :root block:

    **Step 1: Update Surface section** -- Replace existing surface values and add new tokens:
    ```css
    /* Surface */
    --surface-base: #0a0a0f;
    --surface: [STITCH-CONFIRMED Layer 1];
    --surface-container: [STITCH-CONFIRMED Layer 2];
    --surface-container-high: [STITCH-CONFIRMED Layer 3];
    --surface-container-highest: [STITCH-CONFIRMED Layer 4];
    --surface-elevated: [STITCH-CONFIRMED Layer 5];
    ```
    `--surface-base` is ALWAYS `#0a0a0f` (D-01 locked). The other 5 values come from the Stitch session output.

    **Step 1a: Update --surface-elevated placeholder** -- Plan 02 added `--surface-elevated: #131319` as a provisional value. This MUST be updated to the Stitch-confirmed Layer 5 value from Task 1's checkpoint output. The #131319 placeholder must NOT remain unless the Stitch session explicitly confirmed #131319 as the final value.

    **Step 2: Add Neon Glow section** -- Insert AFTER Accent section, BEFORE Surface overlays:
    ```css
    /* Neon Glow (60-30-10) */
    --glow-primary: rgba([R], [G], [B], 0.12);
    --glow-secondary: rgba([R], [G], [B], 0.08);
    --glow-subtle: rgba([R], [G], [B], 0.04);
    ```
    The R, G, B channels come from the `--glow-rgb` value confirmed in Task 1. These are typically derived from `--primary` but may differ if the Stitch session selected a different glow base.

    **Step 3: Add Content-Type Accents section** -- Insert AFTER Neon Glow, BEFORE Surface overlays:
    ```css
    /* Content-Type Accents */
    --accent-anime: [STITCH-CONFIRMED purple];
    --accent-manga: [STITCH-CONFIRMED pink];
    --accent-game: [STITCH-CONFIRMED cyan];
    --accent-news: [STITCH-CONFIRMED amber];
    ```

    **Step 4: Update --primary if changed** -- If the Stitch session updated --primary from the current `#bd9dff`, update it and also update `--primary-soft` and `--primary-glow` rgba values to match the new RGB channels.

    **Step 5: Update html/body background** -- Change the background-color to use `--surface` (which now sits above `--surface-base`):
    ```css
    html, body, #root {
      min-height: 100vh;
      background-color: var(--surface);
    }
    ```
    (This should already be correct from the existing code, but verify.)

    **Step 6: Check line count** -- Count total lines. If exceeding 280, split into palette.css + typography.css as documented in RESEARCH.md Architecture Patterns. Import order: `@import "./palette.css";` then `@import "./typography.css";` before animations.css and components.css.

    IMPORTANT: Do NOT use any HEX value that was not explicitly confirmed in Task 1's checkpoint output. All `[STITCH-CONFIRMED]` placeholders MUST be replaced with the actual values from the checkpoint.
  </action>
  <verify>
    <automated>grep -c "131319" src/styles/globals.css | grep -q "^0$" && echo "placeholder removed" || echo "FAIL: #131319 placeholder still present -- must be replaced with Stitch-confirmed value unless checkpoint approved it" ; npm run check && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/styles/globals.css` contains `--surface-base: #0a0a0f;`
    - `src/styles/globals.css` contains `--surface-elevated:` with the Stitch-confirmed HEX value from Task 1 checkpoint
    - `grep -c "131319" src/styles/globals.css` returns 0 (Plan 02 placeholder replaced -- unless Task 1 checkpoint explicitly approved #131319)
    - `src/styles/globals.css` contains `--glow-primary: rgba(`
    - `src/styles/globals.css` contains `--glow-secondary: rgba(`
    - `src/styles/globals.css` contains `--glow-subtle: rgba(`
    - `src/styles/globals.css` contains `--accent-anime:`
    - `src/styles/globals.css` contains `--accent-manga:`
    - `src/styles/globals.css` contains `--accent-game:`
    - `src/styles/globals.css` contains `--accent-news:`
    - `npm run check` exits 0
    - `npm run typecheck` exits 0
    - File line count is under 300 (or split into palette.css + typography.css if needed)
  </acceptance_criteria>
  <done>All palette tokens written with Stitch-confirmed values. 5-layer surface hierarchy, 3-tier glow system, and 4 content-type accents all defined as CSS variables. --surface-elevated updated from #131319 placeholder to Stitch-confirmed value. Biome and TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite design.md with complete new design system documentation</name>
  <files>design.md</files>
  <read_first>
    - design.md (current v1.0 design system -- this will be completely rewritten)
    - src/styles/globals.css (current state after Task 2 -- the source of truth for all token values)
    - .planning/phases/04-design-token-foundation/04-UI-SPEC.md (Typography, Color, Spacing sections -- reference for documentation structure)
    - .claude/rules/design-system.md (naming conventions, forbidden patterns -- must be reflected in design.md)
  </read_first>
  <action>
    Completely rewrite `design.md` (per DTKN-07) to document the new v2.0 design system. The document must serve as the single source of truth for all CSS token values.

    Structure the document with these sections:

    **1. Brand Identity** -- Update tone to "void-black cyberpunk with neon glow accents". Keep dark-only, Tauri v2 desktop app context.

    **2. Color Tokens** -- Document every CSS variable from globals.css :root:
    - **Surface Hierarchy (5 layers)**: Table with token name, HEX value, semantic role. Include --surface-base as Layer 0.
    - **Content (text/outline)**: --on-surface, --on-surface-variant, --outline, --outline-variant
    - **Accent**: --primary and family, --secondary, --tertiary, --error
    - **Neon Glow System**: --glow-primary, --glow-secondary, --glow-subtle with opacity rationale (60-30-10)
    - **Content-Type Accents**: --accent-anime, --accent-manga, --accent-game, --accent-news with hue family and WCAG AA contrast ratio vs --surface-container
    - **Surface Overlays**: --surface-hover, --surface-hover-strong, --surface-active, --surface-glass, --surface-backdrop
    - **Shadows**: --shadow-md, --shadow-lg
    - **Inherited Tokens**: mark which tokens are carried forward unchanged from v1.0

    **3. Typography** -- Document the font system:
    - Font family: Noto Sans JP Variable (--font-jp)
    - Weight tokens: --font-weight-light (300), --font-weight-regular (400), --font-weight-semibold (600)
    - Type scale: meta=11px/300, body=13px/400, card-title=15px/600, heading=18px/600

    **4. Spacing & Layout** -- Carry forward from current design.md (8-point grid, xs through 3xl, TopBar 44px exception)

    **5. Design Rules** -- Document:
    - 60-30-10 color distribution rule
    - WCAG AA 4.5:1 minimum contrast for all accent text
    - No hardcoded HEX/RGB -- all colors through CSS variables
    - No Tailwind default colors -- only CSS variable references via bg-(--var) syntax
    - No inline styles -- Tailwind classes only
    - No light mode -- dark-only :root
    - Blur budget: max 2 blurred elements, each under 15% viewport
    - Decoration budget: 1 animated + 1 gradient + 1 decorative icon per component
    - Animation budget: 1 entrance animation at a time; ambient off by default

    **6. Stitch Token Mapping** -- Full conversion table:
    - Stitch output token -> CSS variable -> Tailwind arbitrary syntax
    - Example: `Stitch #0a0a0f -> --surface-base -> bg-(--surface-base)`

    **7. Legacy Alias Status** -- Note: "All 16 legacy aliases removed in Phase 4 (v2.0). No migration needed for new code."

    Keep the document concise -- tables over prose. Each token table must include: token name, current value, and usage description.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `design.md` contains `## 1. Brand Identity` section
    - `design.md` contains `--surface-base` with value `#0a0a0f`
    - `design.md` contains `--glow-primary` in the Color Tokens section
    - `design.md` contains `--accent-anime`, `--accent-manga`, `--accent-game`, `--accent-news`
    - `design.md` contains `Noto Sans JP Variable` in Typography section
    - `design.md` contains `--font-weight-light` with value `300`
    - `design.md` contains `Stitch Token Mapping` section
    - `design.md` contains `WCAG AA` or `4.5:1` contrast reference
    - `design.md` contains `60-30-10` rule documentation
    - `design.md` does NOT contain any legacy alias names (--bg-card, --text-primary, etc.) except in the Legacy Alias Status note
    - `design.md` file size is under 300 lines
  </acceptance_criteria>
  <done>design.md fully rewritten with complete v2.0 design system. All token tables populated with Stitch-confirmed values. Stitch Token Mapping updated. WCAG contrast ratios documented.</done>
</task>
</tasks>

<verification>
1. `npm run check` passes (Biome clean)
2. `npm run typecheck` passes (TypeScript clean)
3. `npx vitest run` passes (all 157 existing tests green)
4. `grep -n "surface-base" src/styles/globals.css` returns the #0a0a0f definition
5. `grep -n "glow-primary" src/styles/globals.css` returns the rgba definition
6. `grep -n "accent-anime" src/styles/globals.css` returns the accent definition
7. `grep -n "accent-game" src/styles/globals.css` returns the cyan accent
8. `grep -c "131319" src/styles/globals.css` returns 0 (placeholder replaced with Stitch value)
9. `grep -n "Noto Sans JP Variable" design.md` returns typography reference
10. `grep -n "Stitch Token Mapping" design.md` returns section header
11. Every accent color passes WCAG AA 4.5:1 against --surface-container (verified during Stitch session checkpoint)
</verification>

<success_criteria>
- Void-black (#0a0a0f) surface-base defined; 5 distinct surface layers committed with Stitch-confirmed values
- --surface-elevated updated from Plan 02 placeholder (#131319) to Stitch-confirmed value
- Neon glow system (3 tiers) defined as CSS rgba variables
- 4 content-type accent colors defined, all passing WCAG AA 4.5:1
- design.md completely rewritten to document v2.0 token system
- Zero Biome errors, zero TypeScript errors, all 157 tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-design-token-foundation/04-03-SUMMARY.md`
</output>
