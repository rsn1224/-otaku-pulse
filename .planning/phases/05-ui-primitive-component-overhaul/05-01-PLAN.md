---
phase: 05-ui-primitive-component-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/lib/utils.ts
  - src/components/ui/Badge.tsx
  - src/components/ui/Button.tsx
  - src/components/ui/Spinner.tsx
  - src/components/ui/Input.tsx
  - src/components/ui/ToggleGroup.tsx
  - src/components/ui/Card.tsx
  - src/components/ui/Modal.tsx
  - src/styles/components.css
autonomous: true
requirements: [COMP-01]

must_haves:
  truths:
    - "All 7 UI primitives use cva() + cn() pattern instead of manual VARIANT_CLASSES + .join()"
    - "Badge has 10 variants including ai, content-anime, content-manga, content-game, content-news"
    - "Button has 6 variants including neon and glass"
    - "Card has 3 variants: default, glass, featured"
    - "Modal dialog panel uses bold glass (blur 20px + white/15% border)"
    - "cn() utility exists as single source in src/lib/utils.ts"
    - "All 7 primitives use function declarations, not React.FC"
    - "Existing test suite (76 JS tests) passes without modification"
  artifacts:
    - path: "src/lib/utils.ts"
      provides: "cn() utility function"
      exports: ["cn"]
    - path: "src/components/ui/Badge.tsx"
      provides: "Badge with CVA variants including ai and content-type"
      exports: ["Badge", "badgeVariants"]
    - path: "src/components/ui/Button.tsx"
      provides: "Button with CVA variants including neon and glass"
      exports: ["Button", "buttonVariants"]
    - path: "src/components/ui/Card.tsx"
      provides: "Card with default/glass/featured variants"
      exports: ["Card", "cardVariants"]
    - path: "src/components/ui/Modal.tsx"
      provides: "Modal with bold glass dialog panel"
      exports: ["Modal"]
  key_links:
    - from: "src/components/ui/Button.tsx"
      to: "src/lib/utils.ts"
      via: "import { cn } from"
      pattern: "import.*cn.*from.*lib/utils"
    - from: "src/components/ui/Badge.tsx"
      to: "class-variance-authority"
      via: "cva() call"
      pattern: "import.*cva.*from.*class-variance-authority"
    - from: "src/components/ui/Modal.tsx"
      to: "src/styles/components.css"
      via: "bold-glass class or inline Tailwind blur"
      pattern: "backdrop-blur|bold-glass"
---

<objective>
Install CVA/clsx/tailwind-merge/lucide-react packages and migrate all 7 UI primitives (Badge, Button, Spinner, Input, ToggleGroup, Card, Modal) from manual `VARIANT_CLASSES + .join(' ')` pattern to CVA-based variant system with new design variants (neon, glass, ai, content-type). Add bold-glass CSS utility for glassmorphism.

Purpose: Establishes the component variant foundation (per D-01, D-02) that all feature components build upon.
Output: 7 rebuilt primitives + cn() utility + bold-glass CSS + 4 new npm packages installed.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ui-primitive-component-overhaul/05-CONTEXT.md
@.planning/phases/05-ui-primitive-component-overhaul/05-UI-SPEC.md
@.planning/phases/05-ui-primitive-component-overhaul/05-RESEARCH.md
@design.md
@.claude/rules/design-system.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install packages and create cn() utility</name>
  <files>package.json, src/lib/utils.ts</files>
  <read_first>
    - package.json (current dependencies)
    - src/lib/utils.ts (may not exist — create new)
  </read_first>
  <action>
1. Install 4 new packages:
   ```bash
   npm install class-variance-authority@0.7.1 clsx@2.1.1 tailwind-merge@3.5.0 lucide-react@1.7.0
   ```

2. Create `src/lib/utils.ts` with the cn() utility:
   ```typescript
   import { clsx, type ClassValue } from 'clsx';
   import { twMerge } from 'tailwind-merge';

   export function cn(...inputs: ClassValue[]): string {
     return twMerge(clsx(inputs));
   }
   ```

3. Add bold-glass CSS utilities to `src/styles/components.css` at the top (before existing component styles):
   ```css
   /* Bold Glass — glassmorphism base */
   .bold-glass {
     backdrop-filter: blur(20px);
     -webkit-backdrop-filter: blur(20px);
     background: var(--surface-glass);
     border: 1px solid rgba(255, 255, 255, 0.15);
   }

   .bold-glass-sm {
     backdrop-filter: blur(16px);
     -webkit-backdrop-filter: blur(16px);
     background: var(--surface-glass);
     border: 1px solid rgba(255, 255, 255, 0.15);
   }
   ```
  </action>
  <verify>
    <automated>cd /c/dev/otaku-pulse && node -e "require('class-variance-authority'); require('clsx'); require('tailwind-merge'); require('lucide-react'); console.log('OK')" && npx tsc --noEmit --strict src/lib/utils.ts 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "class-variance-authority": "0.7.1"
    - package.json contains "clsx": "2.1.1"
    - package.json contains "tailwind-merge": "3.5.0"
    - package.json contains "lucide-react": "1.7.0"
    - src/lib/utils.ts contains `export function cn(`
    - src/lib/utils.ts contains `import { clsx` from 'clsx'
    - src/lib/utils.ts contains `import { twMerge }` from 'tailwind-merge'
    - src/styles/components.css contains `.bold-glass {`
    - src/styles/components.css contains `backdrop-filter: blur(20px)`
    - src/styles/components.css contains `.bold-glass-sm {`
    - src/styles/components.css contains `backdrop-filter: blur(16px)`
  </acceptance_criteria>
  <done>All 4 packages installed, cn() utility compiles, bold-glass CSS utilities defined.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate all 7 UI primitives to CVA pattern with new variants</name>
  <files>
    src/components/ui/Badge.tsx,
    src/components/ui/Button.tsx,
    src/components/ui/Spinner.tsx,
    src/components/ui/Input.tsx,
    src/components/ui/ToggleGroup.tsx,
    src/components/ui/Card.tsx,
    src/components/ui/Modal.tsx
  </files>
  <read_first>
    - src/components/ui/Badge.tsx (current implementation)
    - src/components/ui/Button.tsx (current implementation)
    - src/components/ui/Spinner.tsx (current implementation)
    - src/components/ui/Input.tsx (current implementation)
    - src/components/ui/ToggleGroup.tsx (current implementation)
    - src/components/ui/Card.tsx (current implementation)
    - src/components/ui/Modal.tsx (current implementation — has a11y hooks, keep them)
    - src/components/ui/index.ts (barrel exports — do not break)
    - src/lib/utils.ts (cn utility just created)
    - .planning/phases/05-ui-primitive-component-overhaul/05-UI-SPEC.md (exact variant specs)
    - .planning/phases/05-ui-primitive-component-overhaul/05-RESEARCH.md §CVA Migration Pattern
  </read_first>
  <action>
Migrate each primitive from `const VARIANT_CLASSES + .join(' ')` to `cva()` + `cn()`. Change `React.FC<Props>` to function declarations. Add new design variants per UI-SPEC.md. Export both the component and its variants function (e.g., `badgeVariants`).

**Badge.tsx** — per D-01, COMP-01, UI-SPEC:
- Import: `import { cva, type VariantProps } from 'class-variance-authority';` and `import { cn } from '../../lib/utils';`
- Create `badgeVariants = cva(base, { variants: { variant: { ... } } })` with these variants:
  - `default`: `bg-white/[0.04] text-(--on-surface-variant) border-(--surface-container-highest)`
  - `category`: `bg-(--primary-glow) text-(--primary) border-[rgba(29,185,160,0.15)]`
  - `hot`: `bg-(--error)/10 text-(--error) border-(--error)/20`
  - `new`: `bg-(--secondary)/10 text-(--secondary) border-(--secondary)/20`
  - `count`: `bg-(--primary) text-white border-transparent`
  - `ai`: `bg-linear-to-r from-(--accent-anime) to-(--secondary) text-white font-semibold text-[0.625rem]` (purple-to-blue gradient, per COMP-05)
  - `content-anime`: `border-(--accent-anime) bg-(--accent-anime)/10 text-(--accent-anime)`
  - `content-manga`: `border-(--accent-manga) bg-(--accent-manga)/10 text-(--accent-manga)`
  - `content-game`: `border-(--accent-game) bg-(--accent-game)/10 text-(--accent-game)`
  - `content-news`: `border-(--accent-news) bg-(--accent-news)/10 text-(--accent-news)`
- Base classes: `inline-flex items-center gap-1 border rounded-full font-medium tracking-wide`
- Sizes for count vs other handled via compound variants or conditional logic
- Change to function declaration: `export function Badge({ variant, children, className }: BadgeProps): React.JSX.Element`
- Export `badgeVariants`

**Button.tsx** — per D-01, UI-SPEC:
- Create `buttonVariants = cva(base, { variants: { variant: {...}, size: {...} }, defaultVariants: { variant: 'secondary', size: 'md' } })`
- Existing variants: `primary`, `secondary`, `ghost`, `danger` (keep exact classes)
- New variant `neon`: `border border-(--primary) bg-(--primary-glow) text-(--primary) hover:shadow-[0_0_16px_var(--glow-primary)]`
- New variant `glass`: `bg-(--surface-glass) border border-white/15 text-(--on-surface) backdrop-blur-[20px]`
- Sizes: sm/md/lg (keep exact current classes)
- Base classes include: `inline-flex items-center justify-center rounded-lg font-medium transition-all duration-150 active:scale-95 focus:outline-none focus-visible:ring-2 focus-visible:ring-(--primary) focus-visible:ring-offset-1 focus-visible:ring-offset-(--surface) disabled:opacity-50 disabled:pointer-events-none`
- Change `React.FC<ButtonProps>` to `export function Button(...): React.JSX.Element`
- Props: `extends React.ButtonHTMLAttributes<HTMLButtonElement>, VariantProps<typeof buttonVariants>` + `isLoading?: boolean`
- Export `buttonVariants`

**Spinner.tsx** — minimal change:
- Wrap existing SIZE_MAP in cva if desired, or keep simple since it only has size variants
- Update spinner color: border color `border-(--outline-variant)` and top border `border-t-(--primary)` (already correct)
- Already uses function declaration — no change needed there

**Input.tsx** — per UI-SPEC:
- Keep `forwardRef` pattern and `displayName`
- Upgrade wrapper div classes: `bg-(--surface-container)` instead of `bg-white/[0.04]`, border `border-(--outline-variant)` instead of `border-(--surface-container-highest)`
- Focus state: `focus-within:border-(--primary) focus-within:shadow-[var(--focus-ring)]`
- Error state: `border-(--error)` (already correct)
- Add error helper text: `text-[0.6875rem] text-(--error)` (11px per UI-SPEC)
- Border radius: `rounded-lg` (0.5rem — already correct)

**ToggleGroup.tsx** — per UI-SPEC:
- Active item: `bg-(--primary-soft) text-(--primary) font-semibold` with `border-b-2 border-b-(--primary)` (already correct)
- Use cn() for class composition instead of .join(' ')
- Already uses function declaration — no change needed there

**Card.tsx** — per UI-SPEC:
- Create `cardVariants = cva(base, { variants: { variant: { default, glass, featured } } })`
  - `default`: `bg-(--surface-container) border border-(--surface-container-highest)`
  - `glass`: `bold-glass shadow-[var(--shadow-md)]` (uses the CSS utility class)
  - `featured`: `bg-(--surface-container) border-l-4` (left border color applied dynamically via className prop based on content type)
- Base: `relative rounded-[0.875rem] p-6 mb-4 transition-all duration-200`
- Hover for interactive: `hover:bg-(--surface-container-high) hover:shadow-[var(--shadow-md)] hover:-translate-y-0.5`
- Change `React.FC<CardProps>` to `export function Card(...): React.JSX.Element`
- Add `variant` prop to CardProps
- Export `cardVariants`

**Modal.tsx** — per UI-SPEC, COMP-03:
- Keep ALL existing a11y hooks (useFocusTrap, useFocusReturn, useScrollLock) and motion animations
- Overlay: change from `bg-black/70 backdrop-blur-sm` to `bg-(--surface-backdrop)` (no blur on overlay per research recommendation)
- Dialog panel: add bold glass classes — `bold-glass shadow-[var(--shadow-lg)]`
- Border radius: change `rounded-xl` to `rounded-[0.75rem]` (12px per design.md §8)
- Remove `border border-(--outline-variant)` (bold-glass provides its own white/15% border)
- Background: remove `bg-(--surface)` (bold-glass provides `--surface-glass`)
- Use cn() for class composition
- Keep motion variants (modalOverlay, modalContent) unchanged

After all migrations, run `npx biome check --write src/components/ui/` to auto-fix formatting.
  </action>
  <verify>
    <automated>cd /c/dev/otaku-pulse && npm run typecheck && npm run check && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - Badge.tsx contains `import { cva` from 'class-variance-authority'
    - Badge.tsx contains `export function Badge(`
    - Badge.tsx contains variant key `ai`
    - Badge.tsx contains variant key `content-anime`
    - Badge.tsx does NOT contain `React.FC`
    - Badge.tsx does NOT contain `.join(' ')`
    - Button.tsx contains `import { cva`
    - Button.tsx contains `export function Button(`
    - Button.tsx contains variant key `neon`
    - Button.tsx contains variant key `glass`
    - Button.tsx does NOT contain `React.FC`
    - Card.tsx contains `import { cva`
    - Card.tsx contains `export function Card(`
    - Card.tsx contains variant key `glass`
    - Card.tsx contains variant key `featured`
    - Input.tsx contains `import { cn }` from utils
    - Input.tsx contains `bg-(--surface-container)` (not bg-white)
    - ToggleGroup.tsx contains `import { cn }` from utils
    - Modal.tsx contains `bg-(--surface-backdrop)` (overlay)
    - Modal.tsx contains `bold-glass` or `backdrop-blur-[20px]` (dialog panel)
    - Modal.tsx contains `rounded-[0.75rem]`
    - Modal.tsx still contains `useFocusTrap`
    - Modal.tsx still contains `useFocusReturn`
    - Modal.tsx still contains `useScrollLock`
    - `npm run typecheck` exits 0
    - `npm run test` exits 0
  </acceptance_criteria>
  <done>All 7 primitives use CVA pattern with new design variants. No React.FC usage. No .join(' ') pattern. All existing tests pass. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
- `npm run typecheck` — zero TypeScript errors
- `npm run check` — Biome lint/format clean
- `npm run test` — all 76 JS tests pass (especially CardSummary.test.tsx)
- `grep -r "React.FC" src/components/ui/` returns zero matches
- `grep -r "\.join(' ')" src/components/ui/` returns zero matches
- `grep -r "import.*cva" src/components/ui/` returns matches in Badge, Button, Card
</verification>

<success_criteria>
- All 7 primitives migrated to CVA + cn() pattern per D-01
- New variants (ai, content-*, neon, glass, featured) available per UI-SPEC
- Bold-glass CSS utility defined in components.css
- Function declarations replace React.FC across all primitives
- Zero regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-primitive-component-overhaul/05-01-SUMMARY.md`
</output>
