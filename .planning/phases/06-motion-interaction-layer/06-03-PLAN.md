---
phase: 06-motion-interaction-layer
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified: []
autonomous: false
requirements:
  - MOTN-01
  - MOTN-02
  - MOTN-03
  - MOTN-04
  - MOTN-05
  - MOTN-06

must_haves:
  truths:
    - "All 5 ROADMAP success criteria for Phase 6 are met"
    - "npm run typecheck passes"
    - "npm run check passes"
    - "No raw variant imports remain in component files"
  artifacts: []
  key_links: []
---

<objective>
Run automated verification across all Phase 6 changes and present results for human visual inspection.

Purpose: Confirm all 6 MOTN requirements are satisfied and that Phase 6 introduced no regressions.
Output: Verification results + human approval for visual correctness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-motion-interaction-layer/06-CONTEXT.md
@.planning/phases/06-motion-interaction-layer/06-01-SUMMARY.md
@.planning/phases/06-motion-interaction-layer/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated verification of all Phase 6 requirements</name>
  <files></files>
  <read_first>
    - src/lib/motion-variants.ts
    - src/components/layout/AppShell.tsx
    - src/components/wings/ArticleList.tsx
    - src/styles/components.css
    - src/styles/animations.css
  </read_first>
  <action>
    Run the full verification suite. Execute each check and report results:

    **Build checks:**
    1. `npm run typecheck` — must exit 0
    2. `npm run check` — Biome lint/format, must exit 0

    **MOTN-01 (Wing transition):**
    3. `grep "AnimatePresence" src/components/layout/AppShell.tsx` — must match
    4. `grep 'mode="wait"' src/components/layout/AppShell.tsx` — must match
    5. `grep "key={activeWing}" src/components/layout/AppShell.tsx` — must match
    6. `grep "variants.wingTransition" src/components/layout/AppShell.tsx` — must match

    **MOTN-02 (Stagger reveal):**
    7. `grep "staggerChildren: 0.15" src/lib/motion-variants.ts` — must match
    8. `grep "i < 10" src/components/wings/ArticleList.tsx` — must match

    **MOTN-03 (Retro decorations):**
    9. `grep "retro-corner-bracket::before" src/styles/components.css` — must match
    10. `grep "retro-scanline::before" src/styles/components.css` — must match
    11. `grep "retro-decoration::before" src/styles/components.css` — must match
    12. `grep "retro-corner-bracket" src/components/discover/DiscoverCard.tsx` — must match
    13. `grep "retro-decoration" src/components/common/SectionHeader.tsx` — must match

    **MOTN-04 (Hover depth):**
    14. `grep "transform 0.15s ease" src/styles/components.css` — must match (discover-card transition fix)
    15. `grep "hover:-translate-y-px" src/components/ui/Button.tsx` — must match

    **MOTN-05 (Micro-interactions):**
    16. `grep "bookmarkUnpop" src/styles/animations.css` — must match
    17. `grep "likePop" src/styles/animations.css` — must match
    18. `grep "just-bookmarked" src/components/discover/DiscoverCard.tsx` — must match

    **MOTN-06 (Reduced motion):**
    19. `grep "useMotionConfig" src/components/layout/AppShell.tsx src/components/wings/ArticleList.tsx src/components/common/Toast.tsx src/components/ui/Modal.tsx src/components/common/ArticleReader.tsx` — must match in all 5 files
    20. `grep -r "from '../../lib/motion-variants'" src/components/` — must return ZERO matches for variant object imports (type-only imports are fine; `springTransition` direct import is NOT fine)

    **Regression check:**
    21. `cargo test 2>&1 | tail -5` — existing Rust tests pass
    22. Verify no files over 300 lines: check `wc -l` for all modified files

    Report all results in a structured table. If any check fails, fix the issue before proceeding to the human checkpoint.
  </action>
  <verify>
    <automated>cd /c/dev/otaku-pulse && npm run typecheck 2>&1 | tail -3 && npm run check 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - All 22 automated checks pass
    - `npm run typecheck` exits 0
    - `npm run check` exits 0
    - No raw motion-variants variant imports remain in component files
    - All modified files are under 300 lines
  </acceptance_criteria>
  <done>All automated verification checks pass with zero failures; build is clean; no regressions detected</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification of all Phase 6 motion and interaction features</name>
  <files></files>
  <action>
    Present the verification checklist to the user and wait for approval. The user will run `npm run tauri dev` and visually verify all 6 MOTN requirements. No code changes in this task — this is a visual verification gate.
  </action>
  <verify>User types "approved" or provides feedback for issues to fix</verify>
  <done>User has visually verified all 6 MOTN requirements and approved</done>
  <what-built>
    Phase 6 Motion and Interaction Layer:
    - Wing transitions (fade-slide AnimatePresence on tab switch)
    - Card list stagger animation (first 10 items at 150ms intervals)
    - Retro decorations (corner brackets on cards, scanlines on poster images, dot grid on section headers and empty states)
    - Hover depth feedback (translateY lift + shadow on cards and buttons)
    - Bookmark micro-interaction (scale pop on toggle)
    - prefers-reduced-motion guard on all animations
  </what-built>
  <how-to-verify>
    Run `npm run tauri dev` and verify the following:

    1. **Wing transitions (MOTN-01):** Click between Discover/Library/Saved/Schedule/Profile tabs. Each switch should play a smooth 200ms fade + slide-up animation. No snap-replace or flash. No layout jump.

    2. **Stagger reveal (MOTN-02):** On the Discover tab, observe the card list. The first ~10 cards should stagger in with a visible delay between each. Cards further down the list should appear instantly (no delayed animation).

    3. **Retro decorations (MOTN-03):** Look at DiscoverCard — small L-shaped corner brackets should be visible at top-left and bottom-right corners (subtle, purple-tinted). When a card is expanded to poster mode, the cover art image should have faint horizontal scanlines. SectionHeaders should have a subtle dot-grid background pattern.

    4. **Hover depth (MOTN-04):** Hover over a DiscoverCard — it should lift up slightly (2px) with increased shadow, smoothly animated. Hover over Button elements — they should lift 1px. The neon button variant should also show a purple glow ring.

    5. **Bookmark animation (MOTN-05):** Click the bookmark icon on a card — the icon should briefly scale up (pop) and return. Unbookmarking should have a smaller pop.

    6. **Reduced motion (MOTN-06):** To test, enable "Reduce motion" in Windows Settings > Accessibility > Visual Effects > Animation effects = Off. Reload the app. All animations should be eliminated — Wing switches should be instant crossfade, stagger should be instant, hover transitions should be instant.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
All Phase 6 ROADMAP success criteria verified:
1. Wing switching plays 200ms fade-slide AnimatePresence transition with no snap-replace
2. DiscoverCard list staggers at ~150ms for first 10 items; below-fold loads without animation
3. Interactive cards and buttons respond to hover with translateY lift and shadow depth increase; bookmark triggers visible micro-interaction
4. Retro motifs (corner brackets, scanline, dot grid) visible on designated components, CSS-only
5. prefers-reduced-motion removes all entrance/transition animations app-wide
</verification>

<success_criteria>
- All 22 automated checks pass
- Human visual verification approves all 6 MOTN requirements
- No regressions in existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/06-motion-interaction-layer/06-03-SUMMARY.md`
</output>
